<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('../partials/head', { title: 'Log in' }) %>
</head>
<body>
  <div class="auth-page">
    <div class="auth-card">
      <img src="/img/logo.png" alt="Where's My Therapist" />
      <h1>HR Portal</h1>
      <div class="subtitle">Super admin sign-in</div>

      <% if (error) { %>
        <div class="flash flash-error"><%= error %></div>
      <% } %>

      <form method="POST" action="/login">
        <div class="field">
          <label for="email">Email</label>
          <input type="email" id="email" name="email" required autofocus />
        </div>
        <div class="field">
          <label for="password">Password</label>
          <input type="password" id="password" name="password" required />
        </div>
        <button class="btn btn-primary btn-block" type="submit">Log in</button>
      </form>

      <p class="small muted" style="margin-top:18px; text-align:center;">
        Are you an organization's HR contact? <a href="/hr/login">Log in here</a>
      </p>
    </div>
  </div>
</body>
</html>
