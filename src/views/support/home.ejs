<div class="card">
  <div class="card-header"><h2>Support</h2></div>
  <p>There's no support dashboard to browse yet — crisis alerts are emailed directly to the support team, and each alert links straight to the relevant case at <code>/support/user/:userId</code>.</p>
  <p class="small muted">Signed in as <%= currentSupportAgent ? currentSupportAgent.email : '' %>.</p>
</div>
